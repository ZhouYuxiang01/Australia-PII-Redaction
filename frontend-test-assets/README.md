# Frontend Test Assets

This folder is for frontend upload, preview, parsing, redaction, and rendering tests.

All text samples are synthetic. They intentionally include complex formatting and fake PII-like values for testing.

## Structure

- `text/` - prepared text fixtures
- `pdf/` - place PDF fixtures here
- `image/` - place image fixtures here

